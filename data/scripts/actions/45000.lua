-- (Removed) Action script for action id 45000 - obsolete
-- This script was replaced by action id 45500. Keeping a stub to avoid accidental use.

function onUse(player, item, fromPosition, target, toPosition, isHotkey)
    player:sendTextMessage(MESSAGE_STATUS_SMALL, "Ação obsoleta: use o objeto com action id 45500 no mapa.")
    return false
end
